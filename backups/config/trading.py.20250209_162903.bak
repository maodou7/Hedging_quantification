"""
交易配置模块
"""

ARBITRAGE_CONFIG = {
    'min_profit_usdt': 1.0,         # 最小利润（USDT）
    'max_position_size': 10000.0,    # 最大持仓规模（USDT）
    'max_slippage_percent': 0.001,   # 最大允许滑点
    'default_fee_rate': 0.001,       # 默认交易手续费率
    'min_price_diff_percent': 0.001, # 最小价差百分比
    'min_profit_percent': 0.1,       # 最小利润率要求（0.1%）
    'order_book_depth': 20,          # 订单簿深度
    'price_window_size': 20,         # 价格历史窗口大小
    'update_interval': 1,            # 数据更新间隔（秒）
    
    # 风险控制
    'risk_control': {
        'max_drawdown': 0.1,         # 最大回撤
        'stop_loss_rate': 0.02,      # 止损率
        'take_profit_rate': 0.01     # 止盈率
    },
    
    # 执行配置
    'execution': {
        'timeout': 5,                # 执行超时（秒）
        'retry_times': 3,            # 重试次数
        'order_type': 'limit',       # 订单类型：market/limit
        'price_threshold': 0.001,    # 价格阈值（相对于中间价）
        'min_notional': 10.0,        # 最小名义价值（USDT）
        'max_notional': 1000.0       # 最大名义价值（USDT）
    },
    
    # 策略参数
    'strategy': {
        'window_size': 20,           # 移动窗口大小
        'z_score_threshold': 2.0,    # Z分数阈值
        'correlation_threshold': 0.8, # 相关性阈值
        'mean_reversion_threshold': 0.02  # 均值回归阈值
    }
}

# 指标配置
INDICATOR_CONFIG = {
    'sma_periods': [5, 10, 20],      # SMA周期
    'rsi_period': 14,                # RSI周期
    'volatility_window': 20,         # 波动率计算窗口
    'price_history_size': 1000,      # 价格历史记录大小
    'sma_short_period': 5,           # 短期SMA周期
    'sma_long_period': 20,           # 长期SMA周期
    'rsi_overbought': 70,            # RSI超买阈值
    'rsi_oversold': 30,              # RSI超卖阈值
    'update_interval': 60            # 更新间隔（秒）
}

# 策略配置
STRATEGY_CONFIG = {
    # 价差套利策略配置
    'spread_arbitrage': {
        'enabled': True,
        'min_profit_threshold': 0.002,  # 最小利润阈值
        'max_position_size': 10000,     # 最大持仓规模(USDT)
        'trade_amount': 100,            # 每次交易金额(USDT)
        'max_open_orders': 5,           # 最大挂单数量
        'price_decimal': 8,             # 价格小数位数
        'amount_decimal': 4,            # 数量小数位数
        'hedge_mode': 'one_way',        # 对冲模式：one_way/two_way
        'position_mode': 'hedged',      # 持仓模式：hedged/netting
        'order_types': ['limit', 'market'],  # 支持的订单类型
        'time_limit': 60,               # 订单超时时间（秒）
        'min_volume': 1000,             # 最小交易量（USDT）
        'max_spread': 0.01,             # 最大价差（1%）
        'min_spread': 0.001,            # 最小价差（0.1%）
        'update_interval': 1,           # 更新间隔（秒）
    },
    
    # 三角套利策略配置
    'triangle_arbitrage': {
        'enabled': False,
        'min_profit_threshold': 0.003,  # 最小利润阈值
        'max_position_size': 10000,     # 最大持仓规模(USDT)
        'trade_amount': 100,            # 每次交易金额(USDT)
        'max_open_orders': 3,           # 最大挂单数量
        'price_decimal': 8,             # 价格小数位数
        'amount_decimal': 4,            # 数量小数位数
        'execution_mode': 'sequential',  # 执行模式：sequential/parallel
        'timeout': 30,                  # 超时时间（秒）
        'min_volume': 1000,             # 最小交易量（USDT）
        'max_slippage': 0.001,          # 最大滑点（0.1%）
        'update_interval': 1            # 更新间隔（秒）
    },
    
    # 统计套利策略配置
    'statistical_arbitrage': {
        'enabled': False,
        'lookback_period': 100,         # 回看周期
        'z_score_threshold': 2.0,       # Z分数阈值
        'half_life': 10,                # 半衰期
        'correlation_threshold': 0.8,    # 相关性阈值
        'position_size': 1000,          # 持仓规模(USDT)
        'max_positions': 5,             # 最大持仓数
        'stop_loss': 0.02,              # 止损比例
        'take_profit': 0.01,            # 止盈比例
        'update_interval': 5            # 更新间隔（秒）
    },
    
    # 网格交易策略配置
    'grid_trading': {
        'enabled': False,
        'grid_size': 20,                # 网格数量
        'price_range': 0.1,             # 价格范围（10%）
        'investment_amount': 10000,      # 投资金额(USDT)
        'min_grid_profit': 0.001,       # 最小网格利润
        'rebalance_threshold': 0.05,    # 再平衡阈值
        'auto_adjust': True,            # 自动调整网格
        'update_interval': 5            # 更新间隔（秒）
    }
} 